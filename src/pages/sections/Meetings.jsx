import { Box, Typography } from "@mui/material";

function Meetings() {
  return (
    <Box sx={{ width: "100%" }}>
      <Typography variant="h4" component="h1">
        Reuniones
      </Typography>
    </Box>
  );
}

export default Meetings;
